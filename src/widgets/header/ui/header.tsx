import Logo from "@/shared/ui/logo";

const Header = () => {
  return (
    <header className="flex items-center justify-between p-10 h-30 border-b border-gray-200">
      <Logo />
    </header>
  );
};

export default Header;
