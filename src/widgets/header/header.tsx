import Logo from "@/shared/ui/logo";

const Header = () => {
  return (
    <header className="flex items-center justify-between p-4 h-30 border-b border-slate-200">
      <Logo />
    </header>
  );
};

export default Header;
